# 2026-08-13

## 产品图片更新
- 根据用户新添加到 `image/` 目录的产品图片，更新了 `js/data.js` 中 22 款产品的 `images` 字段。
- 高擎动力关节模组：按型号匹配对应 PNG（HTDW/HTPW/HTPU/HTCP 系列）。
- 灵心巧手：按产品名匹配主图及 1-3 张变体图（O6/L6/L20/L20 Lite/L30/O20/EG/TA/A7）。
- 无对应新图的产品（Mini Pi 系列、Panthera-HT、相机/夹爪配件、Linker L25/R30）保持原有图片不变。

## 详情页视频展示
- 用户已将 5 个 Linker Hand 产品视频放入 `image/` 目录。
- 在 `js/data.js` 中为 4 个有对应产品的条目新增 `video` 字段：
  - `linker-hand-o6` → `image/Linker Hand O6_Power Showcase.mp4`
  - `linker-hand-l6` → `image/Linker Hand L6_Full Palm Tactile Pressure Sensing.mp4`
  - `linker-hand-l20-lite` → `image/Linker Hand L20 Lite_Crab Shuttle.mp4`
  - `linker-hand-l30` → `image/Linker Hand L30_Plugging and unplugging the power plug.mp4`
- `Linker Hand O30_Tactile Perception.mp4` 暂时无对应产品，未做关联。
- `product.html` 与 `product.js` 中已有视频展示容器与渲染逻辑，无需额外修改。
- 全站 CSS/JS 缓存版本号从 `20260900` 升级到 `20260901`。
- `js/data.js`、`js/product.js`、`js/include.js` 语法校验通过。
