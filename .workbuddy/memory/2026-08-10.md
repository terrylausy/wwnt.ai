# 2026-08-10 工作日志

## 网站分析
- 全面分析了 WWNT Robotics 仿生机器人电商展示站项目结构
- 纯静态站：HTML5 + CSS3 + 原生 JS，无框架，部署于 Netlify
- 11 个 HTML 页面，核心数据引擎 js/data.js 包含 30+ 真实产品

## 创建 data1.js
- 用户要求：只保留高擎动力 (HighTorque) 和灵心巧手 (LinkerBot) 两个品牌
- 价格用英文格式，全部标注 Ex-work Price
- 数据来源：
  - 高擎动力 xlsx 报价表（USD 价格）
  - 灵心巧手 PDF 产品手册（产品规格）
  - 原 data.js（灵心巧手历史价格）
- 共 32 个产品：HighTorque 21 款 + LinkerBot 11 款
- 新增分类：joint-module（关节模组）、accessory（配件）、teleoperation（遥操作设备）
- 新增字段：currency（USD/CNY）、priceNote（Ex-work Price）
- formatPrice 函数已更新为英文格式输出
- LinkerBot 仅 O6 有具体价格（CN¥6,666），其余为 "Price on Inquiry"

## UI 样式修正
- 修复 Help Center 导航字体与其他 nav-link 不一致的问题
  - 原因：`.nav-dropdown-toggle` 显式覆盖 `font-size: inherit` 和 `font-weight: 500`，覆盖了 `.nav-link` 的 `0.82rem/500`
  - 改为 `font: inherit`，让下拉按钮完全继承 `.nav-link` 样式
- 修复 AI 聊天气泡与回到顶部按钮大小/对齐不一致的问题
  - 统一按钮尺寸为 56×56px，图标 24×24px，右侧对齐 `right: 2rem`
  - 移动端统一为 48×48px（回到顶部 44×44px），右侧对齐 `right: 1.5rem`
  - 调整聊天窗口底部间距为 72px 以匹配新的气泡高度

## 产品图片修正（2026-08-10 后续）
- 确认并更新 6 个产品主图，解决截图中图片错误问题：
  - Mini Pi → `image/hightorque_wrc2025_p1_img1.jpeg`（WRC 2025 宣传单中的 Mini π 双足机器人）
  - Mini Pi Pro → `image/hightorque_wrc2025_p2_img1.jpeg`（同宣传单参数页）
  - Mini Pi Plus → `image/mini-pi-des-1.jpg`（aifitlab 产品页 “Mini Pi+” 渲染图）
  - Mini Pi Plus Pro → `image/mini-pi-plus-performance.png`（高擎官方舞台表演图）
  - ZED Mini → `image/zed_mini_datasheet_page1_render.png`（ZED Mini datasheet 产品图）
  - D435i → `image/d435i_quickstart_page1_render.png`（Intel RealSense D435i 快速指南产品图）
- 顺手修复 Mini Pi Plus Series Gripper 图片：`image/OIP-C.webp` → `image/mini-pi-des-4.jpg`
- 升级全站 CSS/JS 缓存版本号至 `20260840`，避免浏览器继续缓存旧 data.js
- 验证 `js/data.js`、`js/include.js` 语法正常，旧版本号无残留
- 清理临时脚本：`fetch_images.py`、`download_images.py`、`extract_pdf_images.py`、`render_pdf_pages.py`、`get_d435i_image.py`、`fetch_more_images.py`
