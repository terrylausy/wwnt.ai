# 2026-08-11

## Header 样式修复
- 修复了 logo 下方 slogan 被省略的问题（`css/styles.css` `.brand-slogan`）：移除 `text-overflow: ellipsis`，放宽 `max-width` 到 260px，让完整 slogan 可换行显示。
- 修复了 Help Center 导航按钮字体偏大/加粗的问题（`css/styles.css` `.nav-dropdown-toggle`）：显式设置为 `font-size: 0.82rem; font-weight: 500;` 并补齐 padding 与 border-radius，和其他 `.nav-link` 完全一致。
- 为避免浏览器缓存导致样式未生效，将所有 HTML/JS 中引用的资源版本号从 `?v=20260837` 统一升级到 `?v=20260838`（共 11 个文件）。
